services:
  relayserver:
    build:
      context: .
      dockerfile: Dockerfile
    image: dreamcacao/relaydns:latest
    command:
      - "--http-port"
      - "19080"
      - "--p2p-port"
      - "4001"
    network_mode: "host"
    restart: unless-stopped
